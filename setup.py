from setuptools import setup, find_packages

setup(name='seh.pathway_hopping',
      version="0.1",
      packages=['seh_pathway_hopping'],
      package_dir={'' : 'src'},
      package_data={},
      entry_points={
          'console_scripts' : [
          ]}
)
